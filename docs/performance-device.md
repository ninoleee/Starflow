# 真机性能验证

## 内存优先与磁盘协同待验收（2026-09-26）

- 点播目标时长已统一到 120 秒，验证 Exo／MPV 的时长停止点与字节限制先到时的提前停止；iOS 120 秒为 AVPlayer 偏好而非硬上限。检查起播／恢复仍使用短门槛，长缓存没有增加首帧等待，并对比峰值内存与重缓冲。
- TV Exo 验证字节受限时按实际高水位约 75% 补充，不等耗尽；联合记录内存时长、`memoryReady`、`forwardBytes`、源站吞吐和重缓冲次数。MPV 检查设备内核是否支持 hysteresis 秒数／字节选项，iOS 不以精确 75% 作为验收条件。
- 控制栏隐藏时仍应刷新许可；断更超过 4 秒、暂停、拖动、缓冲时停止额外预取。补充内存优先磁盘命中或接管同区间在途下载，验证慢网／慢闪存下不等整块落盘、不重复下载。
- 小容量循环播放、回退／快进以及读取中淘汰，检查前方有效数据、有限回退窗口、读块保护、峰值 PSS 和 IO 延迟。主机策略与网络闸门通过不替代设备测量，见 [主机记录](performance.md#2026-09-26-内存优先与磁盘协同)。

## 网盘账号与转存恢复待验收（2026-09-26）

本节为待执行清单，不是通过报告。主机协议和页面模拟结果见 [performance.md](performance.md)，功能边界见 [阿里保存与转 115](aliyun-to115.md)。

- iPhone 与 Android API 23/ARM32、现代 ARM64 TV：验证旧明文凭据升级迁移、重启读取、安全存储失败保留原值及不回退明文；验证清除登录、换账号后目录重新选择和同步删除保持关闭。iOS Keychain、Android Keystore 和 macOS Keychain 尚未实机验收。
- 阿里扫码：真实 App 确认、取消、过期、后台返回、弱网和相册保存确认框；iPhone 分别测试照片添加允许/拒绝。阿里 App 从相册识别登录二维码的兼容性尚未确认；TV 需实际遥控器和扫码距离验收。
- 使用专用测试目录验证阿里单独保存、115 秒传命中/未命中、停止和进程中断后恢复、跨账号拒绝继续、未确认写入不重放及清理前身份变化。清理必须仅涉及任务记录中的副本；目录内放置不属于任务的测试文件，确认不会递归删除。真实转存、删除、风控和大目录耗时尚未联调。

## 应用更新待验收（2026-09-26）

移除清单密钥后的补验：先手动覆盖安装普通 JSON 模块，再切换发布清单；确认无构建公钥也能检查，旧 payload/signature 包装明确拒绝。验证修改清单证书摘要不能使不同签名的 APK 覆盖已安装应用，HTTPS 证书失败仍拒绝连接。旧客户端不能读取普通 JSON，发布前需确认迁移范围；本轮仅主机回归，未执行此真机流程。

WebDAV 更新源补验：使用“网络同步”的 HTTPS 地址和账号读取同步目录的 `releases/latest.json`，检查中文/嵌套目录、401/403 权限、404 尚未发布、同源 GET 与跨域直链拒绝；下载期间修改同步账号/地址后旧任务释放，旧清单不再安装。原配置/收藏同步仍读写原文件，安装包不会被配置上传动作覆盖。尚未进行真实 WebDAV 服务器联调。

此清单不是通过报告。Android API 23 ARM32 电视和现代 ARM64 手机/电视分别验证：旧正式包覆盖升级后设置、播放历史、收藏和直播库保留；未知来源未授权/授权返回、系统安装器取消/不存在/拒绝安装；慢网、断网、后台、取消后重新下载和空间不足；错误签名、哈希、包名、低版本拒绝；长更新说明在 320px/TV 和大字号下可读，遥控器能进入、取消、重试并返回。确认清单 HTTPS 和 CDN 从真实设备可达、普通 APK 不含配置、原 APK 签名连续，检查失败不打断播放或阻塞启动。记录 APK 实际 versionCode 和设备安装结果，不将“系统界面已打开”当作成功。真机未接入时不得用主机 MockClient/JVM 结果填写通过。部署步骤见 [应用更新](app-updates.md)。

更新日期：2026-09-20。本文定义设备测量方法与验收场景；没有附带本次新采集的 TV、手机或桌面性能结果。主机 smoke、JVM 和 Swift 策略回归见 [performance.md](performance.md)，不能作为真机通过证据。

## 测量前提

- 记录 commit / 工作区差异、应用版本、构建模式、设备型号、OS / API、ABI、内存、屏幕刷新率及音频输出设备。
- 固定网络、代理、服务端版本和同一媒体样本，记录容器、编码、码率、时长、字幕轨及音轨；远程地址和认证头不得放入公开记录。
- 固定首页模块、来源与分区、两个启动刷新开关、共享并发值、批次延迟、界面开关、播放器与解码 / 输出模式。
- 区分首次安装、冷进程、热进程、冷缓存与暖缓存；同一组至少重复多次并保留失败样本。清理缓存或应用数据前先保存重要配置，不用清数据代替可控的冷启动。
- Flutter UI 用 profile 模式和 DevTools 分析；用户侧结果再用相同配置的 release 复核。debug 运行耗时不用于发布性能结论。
- 用户可安装的 TV APK 必须按 [README 发布预设](../README.md) 生成，保持 API 23、ARM32 / ARM64、版本与文件名规范；性能任务不要绕过发布约束。

## 指标口径

| 指标 | 起止与数据来源 | 不应混淆的内容 |
| --- | --- | --- |
| 启动可交互 | 启动应用至首页可操作，配合录屏、原生启动记录和 `app.bootstrap` | Bootstrap 的 10s 从控制器开始算，不覆盖 `main()` 前段、原生初始化或同步阻塞 |
| 首页 / 详情就绪 | 操作至首批可操作内容，分别标记缓存、图片、元数据补全完成 | 不把后台全部刮削完成算作首屏 |
| Flutter UI 帧 | DevTools FrameTiming 的 build / raster、设备 traces | Flutter UI 帧不等于播放器解码帧 |
| 播放启动 | 分开记录点击、解析结束、开流、可见画面及声音，关联 `playback.startup / playback.performance` | 不混入先前页面等待，也不直接跨内核比较不同埋点 |
| 播放稳定性 | 视频掉帧、缓冲次数 / 时长、音频 underrun、A/V 同步、CPU / 内存 | 网络缓冲不一定是 UI 绘制卡顿 |
| seek / 切集 | 输入到目标位置实际稳定播放；区分地址解析、定位和缓冲 | 控件上显示目标时间不代表已经播放到该处 |
| 索引刷新 | 明确增量 / 重建、来源数、条目数、请求量、耗时、最终记录数 | 本地分组成本与远端响应延迟分开记录 |

应用默认长帧监测阈值为 **250ms**，属于严重长帧告警，不是 60Hz / 120Hz 流畅度达标线。没有 `app.performance` 长帧告警也不能推断没有掉帧，应使用完整帧时间分布。

新增 `Flutter frame sample` 日志提供约 600 帧或新帧到达时满 30 秒窗口的 build/raster p50/p95、超 16.667/33.333ms 计数；此计数不等于屏幕实际漏呈现帧数。性能优化复验应补充：慢正文下图片活动数不超过四路、隐藏后释放且重新进入能出图；日志满容量/连续故障；1千/1万/5万 NAS 条目的重建与回访；长会话内存压力和缓存维护；首次/续播/切集/seek 同样本对照。本轮不修改 blur、豆瓣解码例外、播放器缓冲或解码策略，需设备证据后再调参。

Exo 的播放性能数据来自 Media3 Analytics / BandwidthMeter；MPV 读取现有 libmpv 属性，不额外发测速请求。iOS `NativePlaybackMetrics.firstFrameAtIso8601` 当前在 `timeControlStatus == .playing` 时标记，实际是进入播放状态的近似信号，不是已确认视频像素上屏；严格首帧比较需另有录屏或显示层证据。

## 通用场景

| 场景 | 操作 | 重点检查 |
| --- | --- | --- |
| 冷启动 | 无缓存、已有缓存各运行；开启 / 关闭自动刷新分别记录 | 超时是否进入首页、是否重复刷新、焦点是否可操作 |
| 首页与后台 | Hero 翻页、上下滚动、切页、返回、进入后台再恢复 | 图片队列、隐藏页面请求、400ms 恢复等待、保留状态 |
| 详情 | 冷 / 暖历史、慢元数据、多来源多版本、换季、错误 / 空系列 | 按钮不被历史读取隐藏、剧集懒构建、返回后季与滚动不重置 |
| 播放 | 本地与远程同编码样本、首次开流、续播、从头播放 | 原视频与声音、后台任务准入、失败分类与有限重试 |
| 下一集 | 自动边界、手动跨季、预解析命中 / 过期 / 授权失效 | 一次准备、旧结果不污染新会话、失败可手动重试 |
| 跳过边界 | MPV 手机 / 桌面进度条、滑动、双击、TV 遥控器及系统 seek 手动进入片尾；解析中暂停 / 回拖 / 改规则 / 选其他集 | 用户操作优先，迟到结果不切集，恢复后正常续播；记录在途请求是否复用 |
| 短集与完成 | 90 秒片头规则打开 60 秒特辑；片尾边界结束后暂停、退出、重新读取历史 | 短集回到零且正常出帧，不等启动超时；完成标记保留真实位置，手动回拖清除 |
| 保存与索引 | 夸克 / 115 转存、名称修正、STRM 触发、增量刷新 | 重复内容不反复写入、取消边界、刷新时前台仍可操作 |
| 生命周期 | 退出播放、立即重开、后台 / 前台、音频中断、网络变化 | 无叠音、旧实例释放、进度保存、迟到请求不更新新页面 |

“取消”通常意味着停止后续准入和忽略旧结果，不保证已经发送的 HTTP 请求立刻中止。比较请求量时应区分未开始、在途和已完成请求。

## Android TV

至少包括一台低内存 API 23 设备、ARM32 与 ARM64 覆盖，以及可观察 HDMI 能力的现代设备；只有模拟器不足以覆盖这些组合。

### 点播预读与刷新率补验（2026-09-25）

2026-09-26 统一磁盘缓存待验项：MPV／Android Exo／iOS 三档容量、LRU 回读、暂停中清理与再次播放、关闭／切集／画质切换／退出、异常退出残留清理、低剩余空间及闪存失败回退；记录真实磁盘占用、读写延迟、峰值 PSS 和重缓冲次数。覆盖未知扩展名、HLS 点播分片／WebVTT 命中、动态清单／密钥实时请求、本地视频／字幕不复制。检查后台 iOS 代理存活、Range 续接不重复／遗漏、设置入口和字幕清理，区分 loopback 速度与真实远端吞吐。以下历史“本轮仍为内存缓存”指 2026-09-25 批次，不能作为新磁盘层验收结果。

同日后续补强待真机：记录慢闪存下 8 MiB 写队列丢弃量及前台吞吐；对比缓存开／关首帧、重缓冲与峰值内存。有可靠验证器的渐进式媒体应在每轮最多 32 MiB 后继续滚动填充读取游标前方最多 128 MiB，消费后补齐；捕获源站字节量，确认长开放 Range 消费已预读数据而非重复整段下载。HLS 仍最多随后两个 VOD 分片。检查暂停、seek、清理、换集与退出抢占预读，旧响应不覆盖新位置；慢源站不继续扩张。验证至少 512 MiB 剩余空间保护、退出后连接／写队列／目录回落、再次打开不复用旧会话。分别核对 MPV 前向包量、Exo allocator、iOS 内存不可用及当前会话磁盘量，不把磁盘留存量当可播时长。主机覆盖范围拼接、200 回退及清理中读写，但不替代真实文件系统、PiP、后台存活与长时间播放验收；已提交文件操作的收尾延迟须单独测量。

2026-09-26 `15:51:39 / 15:56:13` 导出日志已显示修复前 TV 缓冲耗尽与快进后活动请求积累，分析及主机复现见 [交付阻塞修正](performance.md#2026-09-26-磁盘缓存交付阻塞修正)，不是新实现通过报告。新包重点复测内存达到上限并停读超过 15 秒后连续接上磁盘、慢网小块及时进入内核、连续快进后旧请求及远端吞吐回落。联合记录 `forwardBytes / activeRequests / foregroundNetwork / originBytesPerSecond`、内核缓冲时长、重缓冲和峰值 PSS；已有磁盘命中不能因远处投机下载阻塞，前向量为零时不得以总磁盘占用推断可播数据。

本轮仅有主机测试，以下是真机待验收项，不是效果报告。

- 同一原画片源对比稳定网络、持续低速、短时断流、服务端暂停响应，记录首帧、重缓冲次数／总时长、动态目标、实际下载量、掉帧、音频欠载与峰值 PSS。重点覆盖 1080p 与 4K 高码率；目标字节预算不是进程内存上限。
- 对照 TV Exo 应用内存档 `<=256 / <=512 / >512 MB`：基础目标分别为 `64 MiB`、普通 `96 MiB`／重型或切集 `112 MiB`、`160 MiB`；动态上限分别为 `64 / 128 / 256 MiB`。覆盖码率扩容、内存压力退回基础档与无需填满即可起播／恢复，观察长播内存及 GC 压力，不以盒子标称 RAM 代替应用内存等级。
- TV MPV 覆盖低档普通 `80 MiB`、重型／夸克／激进最多 `112 MiB`，中档最多 `176 MiB`；与 Android Exo 记录区分，MPV 前向包量是 demuxer 缓冲近似值，不代表进程总内存。起播／恢复门槛保持原值。
- 播放中模拟内存压力，核对目标退回基础档且待播数据仍保留，压力冷却结束后可恢复预读；反复 seek、暂停、倍速和切集不沿用旧趋势。检查加载器主动满缓存停读时不误报慢网。
- Android 11+ TV 原生“更多 → 刷新率匹配”开／关，覆盖 23.976、24、25、29.97、50、60fps，记录实际显示刷新率及是否无缝接受；4K 显示播放 1080p 内容不应被当作改变输出分辨率。暂停、1x/1.5x 切换、离开前台、Surface 重建、退出后核对提示已撤销。API 23 设备不出现该开关、不调用新 Surface API。
- MPV 检查普通／慢网／高风险容器的短恢复门槛与持续预读、全屏往返后的参数稳定性，以及掉帧／缓冲时 `playback.health` 快照。各项读取失败保持播放，切集迟到采样不写到新会话。本轮仍为内存缓存，不按磁盘缓存或离线下载验收。

### 直播双内核对比验收（2026-09-20）

状态：真实源/真机未测，已确认 `adb devices` 为空。本节是待执行清单，不是通过报告。五任务合并后的 9 文件/132 项 Flutter、17 项直播 JVM 和 Kotlin 编译结果仅为主机证据；组件截图不执行真实解码，也不能证明设备播放。完整范围见 [主机记录](performance.md#2026-09-20-直播前置验证快照)。

对照条件：同一合法订阅、同一频道/线路/编码、同设备和网络分别运行 MPV 与直播 Exo，交替测试顺序，分开冷/暖进程与首次/再次开流。记录订阅/EPG 更新时间、手动映射、台标缓存状态、播放器选择、日志等级、代理实际路径、API/ABI、可用内存及音频输出。Exo 不继承 Dart 代理、点播 FFmpeg/音频策略或双字幕，不能以点播成功直接填直播结果。先约定允许的失败率及延迟门槛，不以缓冲配置值充当验收阈值。

| 待验收场景 | 对照步骤 | 必须保留的证据 |
| --- | --- | --- |
| 首次开流／换台 | HTTP-HLS 与 HTTP-TS；每个内核至少重复 20 次相同频道切换，包含失败，不只挑最快一次 | 输入、开始开流、首个可见画面、首个声音、稳定播放时间；记录样本数、失败数及成功延迟 p50/p95，失败单列不伪造耗时 |
| 快速输入与焦点 | 画布短按、长按上下/频道键 2/5/10s、反向及首尾循环；确认/左/右/菜单、频道/分组/音轨/线路下拉、节目简介、返回 | 最终选中频道与实际画面一致、无旧事件回写/叠音；叠层方向不换台，工具栏操作不被 5s 隐藏抢焦点，输入到最终稳定播放的时间 |
| 失败及备用线路 | 首线路不可用、全部失败、播放后断网/恢复、慢正文、临时 URL 过期；再显式重试 | 应用层最多三次 2/4/6s 退避、短暂 progress 不重置预算、备用轮换、失败按钮可操作；区分 HLS 子请求/内核加载与应用重开次数 |
| 生命周期／内核切换 | 开流/重连/选轨在途时退出、立即重开、后台/前台、切换内核；来源更新/停用/删除后继续操作 | 同时活动的播放器、声音及网络连接，旧 surface/回调不污染新页，退出后释放趋势；前台恢复不是影视续播或时移 |
| 音轨与显示 | 同源多音轨、静音后换台、内核切换、4:3/16:9/非方形像素流、音频焦点/耳机断开 | 实际声音/静音、宽高比、黑边、A/V 同步、黑屏/有声无画；没有音轨偏好持久化或比例选择时记为未实现，不填通过 |
| 长播与低内存 | 每内核至少连续 60 分钟，含节目切换、弱网恢复、后台往返；另做重复换台/退出重开循环 | CPU、PSS/RSS 与峰值/回收趋势、缓冲次数/时长、视频掉帧及音频 underrun；策略目标不是进程内存上限，需单独观察进程占用 |
| 订阅／EPG／台标 | 1千/1万频道、无 EPG、多日/时区节目、XMLTV.gz、坏数据/超限、慢台标；刷新时收藏/编辑/删除 | 频道/EPG 各自旧缓存保留、迟到结果不复活来源、时间边界、台标失败占位及真实代理/鉴权行为；本地分钟查询不产生网络轮询 |
| 本地持久化／配置隔离 | 重启后查看收藏/隐藏/排序/映射/线路/内核；使用隔离测试资料做配置导出/导入与 WebDAV 同步 | 直播库不进入配置/影视收藏文件，不能当备份恢复；原配置菜单不被强制插入直播。勿用真实数据清库代替验收准备 |
| TV 直播备份扫码（待真机验证） | 同 LAN 手机扫码下载 JSON，再分别上传做合并/替换；无效文件、取消确认、后台、过期及慢网中断 | 手机文件实际保存可再恢复；上传不直接写库，确认取消无变化；关闭/后台无迟到恢复；普通应用配置被拒绝，凭据不进入日志 |
| TV 输入／布局 | 低内存 API 23、ARM32/ARM64；窄屏、横屏，真实遥控器/TV IME/手机扫码上传，初次空库和读库失败 | 焦点可见且可离开、长按确认不重复弹窗、删除默认取消、列表刷新后有操作入口；扫码无效文件可重试、关闭/后台不再接收、成功仍需电视保存；手机/桌面本地选文件另测 |

计时口径：统一以物理输入/点击到实际可见像素及声音为对照，使用录屏、显示层事件或设备 traces。另记“最后一次换台输入到稳定播放”，不要把 180ms 合并等待或队列等待隐藏掉。`live.playback.elapsedMs` 从实际开流任务开始，MPV 首次 progress 与 Exo frame/progress 不能混算像素首帧；直播不自动输出点播的完整 `playback.performance` 摘要，缺少的 decoder、掉帧和音频数据必须靠设备工具补采。

开流挂起专项（待设备验收）：生产适配器已支持队列外 `cancelOpen`，分别记录 15s 超时、取消请求、原生卸载确认、stop/dispose 和下一次 open 的时刻。确认取消回执前不并发重开；模拟原生初始化或卸载本身挂起，界面超时不能作为网络已中止的证据。无取消能力的适配器仍等待旧 open settle。修复前的 19 项生命周期/6 项通道结果以及新增可取消 fake 测试均不代替此设备场景。

数据边界补验：10000 频道、单频道 64 线路、全表 50000 线路的临界与超限输入，EPG 失败保留台标/节目、手动排序后新增追加、删除/禁用/同 ID 重建时迟到回复隔离，以及来源归属偏好清理。后续数据 38 项通过不替代这些规模在低内存设备上的峰值/耗时与真实源验证，不与前置结果相加。

每组填写日期、代码快照、设备/内核、样本别名、操作、次数、失败率、p50/p95、峰值内存及原始采样来源；同时标记未支持/未测/失败，不能将缺失值填 0。统计方法保持一致并保留全部成功原始样本，20 次的 p95 仍是有限样本估计。地址、headers、token 及订阅正文不进入公开记录。配置备份不包含直播数据库，任何破坏性测试前须单独保留测试输入和必要数据。

本节不要求本次文档任务构建或安装 APK；后续确需用户安装包仍遵守 README 发布预设，不能交付原始 debug/release 文件。直播暂不按暂停时移、回看/录制、外挂字幕/双字幕、画面比例选择、PiP 或飞牛/Emby 直播库验收，这些没有应用入口。

### 遥控器与选集

以下为点播场景，独立直播见上一节；直播没有 seek 或选集入口。

- 控制栏隐藏 / 显示、暂停 / 缓冲状态下分别短按及长按左右 2 / 5 / 10 秒，检查 250ms 合并窗口、松手立即提交和 10 / 30 / 60 / 120 秒加速档。
- 长按中反向、Back / Menu / 确认、焦点丢失和切集，确保旧待执行 seek 不落到新一集。
- 选集面板覆盖单季、多季、列表 / 网格、30 条分段和定位当前集；记录焦点、滚动、请求数和按键到画面恢复时间。
- 分开统计控制层帧时间与解码掉帧；选集外观的 JVM 源码断言失败需先定位，但不直接证明设备帧率回退。

### 音频、TS 与字幕

- AC-3、E-AC-3 / JOC、TrueHD、DTS / DTS-HD、MP1 / MP2 / MP3，覆盖缺失编码元数据和多音轨跨编码切换。
- 自动 / PCM / 设备直通分别测试，记录实际 decoder、输出 encoding、采样率和声道，不把菜单选择等同于实际直通。
- Blu-ray LPCM 覆盖布局 1 / 3 / 9 / 11、48 / 96 / 192kHz、16 / 20 / 24-bit；reader 保留 PCM16/PCM24。自动/设备直通正常速度检查高位深转 float，PCM 兼容和倍速检查 PCM16，其他布局应明确失败，不能无限准备。
- 高精度专项（2026-09-20，待真机）：1x -> 1.5x -> 1x、暂停时改速、音量变化、飞牛画质重开、同码率不同位深切轨、HDMI 插拔及不支持输出时单次回退。核对进度/音轨/暂停/音量不丢失，输入低位保留的主机测试不能替代 AudioTrack 和最终接收设备验收；本轮未采集 bit-perfect 或设备性能结果。
- 输出策略补强（2026-09-20，待真机，adb 无已连接设备）：AAC 系统解码 PCM16 连续改速应无重连；DTS/AC3 实际直通在改速后应转解码输出、恢复 1x 后重新具备直通资格；非默认音调同样检查。模拟压缩输入但 float sink 的初始化/写入失败，确认仅一次 PCM16 回退且不误用 FFmpeg 解码预算；已为 PCM16 的输出失败不循环重试。快速切轨/退出重开及 HDMI 插拔检查旧实例事件隔离，结合日志核对真实 decoder、输入/输出标签和重建决定。
- 同日五项边界修复验收：支持 float 的系统 FLAC/AAC decoder 执行 1x -> 1.5x -> 1x，确认恢复原输出且只有必要的重连；原生 PCM16 与 FFmpeg AC-3 不误恢复。同格式切轨/AudioTrack 复用时保留输出编码，旧释放事件不得清空新输出。拒绝高精度输出的设备检查配置阶段失败能否单次回退；DRM/未知源错误保持保守，不把主机注入的异常与状态序列测试当作设备成功测量。
- 检查音频单次回退后的进度、暂停、倍速、音量和选轨；确认网络 / 视频错误不被误当音频回退，视频解码策略不变。
- 对高码率 progressive TS 执行长距离 seek 和续播，核对实际画面 / 字幕时间；PCR 搜索窗增大可能增加既有 Range 流量，需同时记录请求量与耗时。
- 内封文本 / PGS、双字幕、在线与本地外挂、编码及偏移分别测试；快速 seek / 换轨 / 退出后不得补播旧 cue，PGS 坐标、长时显示和清屏需目视确认。
- 位图加固后的专项验收：PGS 普通组合复用、多对象/裁剪、调色板动画、连续 seek，VobSub/DVB 正常样本及损坏样本；检查低内存 API 23 设备的峰值内存。MPV 验证 auto、迟到 track-list、位图与文本互切，确认原生与 Flutter 不重复显示。主机合成样本和 JVM Bitmap mock 不能替代这些画面结果，目前没有本次改动的新增设备测量。
- Android TV 不把手机的原生 PiP 能力作为验收项；手机 PiP、通知与媒体按键另测。

本地 LPCM 样本与 FFmpeg 逐字节对照命令见 [主机验证](performance.md)，二进制来源见 [音频依赖](../android/app/libs/README.md)。正确字节输出并不能证明设备音频输出兼容或 CPU 足够。

## iOS 与其他平台

- iOS MPV 与 AVPlayer 分开记录。AVPlayer 系统字幕轨与新增外挂文本叠层分别验收：本地／在线 SRT、ASS、SSA、VTT，拖动／换集／取消／退出不残留旧 cue 或下载文件。外挂叠层不等于系统字幕轨，PiP 不显示外挂文本，不按双字幕或完整 ASS 动画功能验收。
- iPhone / iPad 检查锁屏、控制中心、后台开关、音频中断、路由变化、原生 PiP，以及封面失败回退；不能只验证 Swift 语言策略。
- 飞牛转码需真实 NAS 检查 HLS 绝对时间轴、seek、换轨新旧会话切换、退出后的资源释放。既有只读层级接口联调没有覆盖这些行为。
- 桌面 MPV 与外部播放器分开验证；外部启动成功不等于应用持有播放状态。macOS / Windows / Linux 的音频解码能力需按各自打包依赖验证，Android / iOS full 覆盖不自动扩展到桌面。
- Web 使用浏览器媒体后端，受格式、自动播放与 CORS 约束；IO 在线字幕仓库、文件字幕选择及文件日志能力不可直接套用。

## 采集与记录

结构化本地日志、预览、筛选、清理和导出仍启用；旧 trace helper 静音不代表关闭日志。比较两组时保持日志等级和容量一致。Android API 30+ 可补充系统历史退出信息，老设备仍有应用原生日志。

Android 可使用 `adb shell dumpsys meminfo com.example.starflow` 采集进程内存，并结合 Perfetto / Android Studio profiler 分析；iOS 使用 Instruments。`gfxinfo` 等 UI 指标不能单独代表 Flutter raster 或独立视频 surface 的全部成本。工具采样本身可能增加开销，应记录是否启用。

每份结果至少包含：日期、版本 / 差异、设备与构建、样本身份、缓存与网络条件、配置、操作步骤、原始采样、统计方法、失败数、结论与仍未覆盖项。公开附件先检查地址、用户名、分享码和凭据；不要提交受版权限制的媒体样本或未脱敏日志。

当前状态：本文是验收方法，不是通过报告；2026-09-20 文档整理未连接设备测量。
