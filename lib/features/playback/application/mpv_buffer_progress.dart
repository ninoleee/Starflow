import 'playback_buffer_progress.dart';
export 'playback_buffer_progress.dart';

// Compatibility for the native MPV adapter and its existing tests.
typedef MpvBufferProgress = PlaybackBufferProgress;
